# 更正与重测记录（2026-09-12 晚，数据已整体替换）

本目录自身配置（wp=0 显式关闭、pv 默认16）均为有效取值，原始数据本不受 wp 哨兵错误影响；
但 (1) 旧 summary 首行错标 randomPlies（硬编码错误），(2) 其对照臂 evalab_A 的数据无效，
使"wp=150 vs wp=0"对照不成立。为获得同一二进制、同一时段的一致三臂数据，
本目录于 2026-09-12 晚用修复后的驱动**整体重跑并替换**。
错误的完整分析见 git 历史中本文件的上一版本及 evalab_A_purebest/CORRECTION.md。

## 原始错误摘要（两处，均已修复）

1. summary 标签硬编码 `randomPlies=10+gap=30`，与实际（纯最优）不符。
2. 对照臂 evalab_A 传入的 `-1` 没有"默认150"哨兵语义，被原样写入（≈关闭）——
   旧 A vs B 实际是 **wp=-1 vs wp=0**，两者都近乎关闭，6 胜的比分差纯属噪声。

修复（提交 694b925）：summary 标签按实际开关生成；`wp>=0` 才赋值，`wp=-1` 表示保留默认 150。

## 重测配置

- 命令：`bin\AIBenchmarkMay.exe 0 3 20 10 10 10000 8 1 0 -1`
  （规则0–3 × 20局；5月引擎 d78811a 深度10 单线程 vs 当前版深度10 八线程；
  每步限10秒、100命令判和、奇偶局交替执先；pureBest=1（关随机）；
  wp=0 显式关闭胜利临近压力项；pv=-1 → 默认16。）
- 哨兵验证见 `evalab_A_purebest/CORRECTION.md`（wp=-1 ≡ wp=150 逐字节相同，wp=0 有实质分歧）。
- 新 summary 首行：`随机=关 wp=0 pv=默认16`。
- 80 局全程零强制出招、零拒绝、零模型分歧。

## 重测结果（5月胜:新胜:和棋）

| 规则0 | 规则1 | 规则2 | 规则3 | 合计 |
|---|---|---|---|---|
| 7:2:11 | 6:14:0 | 1:7:12 | 4:3:13 | **18:26:36** |

执先拆分（每格为 5月胜/新胜/和棋）：

| 规则 | 5月执先的10局 | 新执先的10局 |
|---|---|---|
| 0 | 3/2/5 | 4/0/6 |
| 1 | 6/4/0 | 0/10/0 |
| 2 | 1/4/5 | 0/3/7 |
| 3 | 2/1/7 | 2/2/6 |

## 修正结论

- **噪声基线**：本臂同配置两跑相差 5 胜（旧 15:31:34 / 新 18:26:36；驱动改动不影响
  wp=0 语义，引擎副本仅编译结构修补、行为不变），给出 80 局单次跑分的固有噪声量级。
- **"winPressure=150 是真实的负资产"撤回**：A(150) 29胜 vs 本臂(0) 26胜，Δ=3 < 噪声基线。
- **"pointValue=16 是正资产"同样不成立**：C(pv=0) 25胜 vs 本臂(pv=16) 26胜，Δ=1；
  旧数据 B→C 的 31→25（6 胜差距）是噪声。
- 三臂纯最优（17:29 / 18:26 / 20:25）彼此难分；当前版默认配置（wp=150+pv=16）
  无需按本组实验调整。
